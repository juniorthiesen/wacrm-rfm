interface:
  display_name: "Wacrm Rfm"
  short_description: "Repo-specific patterns and workflows for wacrm-rfm"
  default_prompt: "Use the wacrm-rfm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true